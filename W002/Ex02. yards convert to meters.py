### Task: ask user for length in yard and convert to meters ###
# 1.094 meters in a yard

length = eval (input ("How length is your length in yards ? "))
meter = round (length/1.094 , 2)
print ('Your yards is', meter, 'meters')
print ( length, 'yards is', meter, 'meters')
print('----------------------------------')
